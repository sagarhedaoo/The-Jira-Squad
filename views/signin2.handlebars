<!DOCTYPE html>
<html>
  <head>
    <title>Login Page</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f2f2f2;
      }
      form {
        background-color: #ffffff;
        border: 1px solid #dddddd;
        padding: 20px;
        margin: 50px auto;
        max-width: 500px;
        box-shadow: 0 0 10px #dddddd;
      }
      label {
        display: block;
        margin-bottom: 10px;
        font-weight: bold;
      }
      input[type="email"],
      input[type="password"] {
        padding: 10px;
        border: 1px solid #cccccc;
        width: 100%;
        margin-bottom: 20px;
        border-radius: 4px;
      }
      input[type="submit"] {
        background-color: #4caf50;
        color: #ffffff;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
      }
      input[type="submit"]:hover {
        background-color: #3e8e41;
      }
      .register-btn {
        display: block;
        text-align: center;
        margin-top: 20px;
        color: #4caf50;
        font-size: 16px;
        text-decoration: none;
      }
      .register-btn:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <form>
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required />

      <label for="password">Password:</label>
      <input type="password" id="password" name="password" required />

      <input type="submit" value="Login" />

      <a href="http://localhost:3000/signup" class="register-btn">Register</a>
    </form>
  </body>
</html>
